# Historical Local-Persistence Source

Status: inactive provenance only.

The imported upstream bundle retained a tombstone for a superseded
database-primary synchronization model named `local-persistence`. That model
is not part of this repository and none of its synchronization instructions,
tools, services, or automatic mutation behavior may be executed or reinstated.

Current policy is defined by root `AGENTS.md` and
[`../../../SKILL.md`](../../../SKILL.md): Git worktree files under the canonical
skills source are authoritative, deployed files are generated copies, and
maintenance remains source-only unless deployment is explicitly in scope.

The adjacent [`metadata.json`](metadata.json) is retained only to identify the
historical source. It is not OpenCode configuration or active skill metadata.
