{
  "name": "opencode-codex-fallback-local",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OpenCode server plugin that fails over from the Codex subscription to a configurable provider chain.",
  "scripts": {
    "check": "npm run typecheck && npm test",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@opencode-ai/plugin": "1.18.31"
  },
  "devDependencies": {
    "@types/node": "22.18.6",
    "typescript": "5.9.3"
  }
}
